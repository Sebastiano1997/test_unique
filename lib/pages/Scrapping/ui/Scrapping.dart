import 'package:flutter/material.dart';

class ScrappingPage extends StatelessWidget {
  const ScrappingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Scrapping')),
      body: const Center(child: Text('Placeholder per Scrapping')),
    );
  }
}
