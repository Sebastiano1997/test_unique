import 'package:flutter/material.dart';

class EgiDartPad extends StatelessWidget {
  const EgiDartPad({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('EGI DartPad')),
      body: const Center(child: Text('Placeholder per EGI DartPad')),
    );
  }
}
